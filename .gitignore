# Dependencies
node_modules/
.pnpm-store/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build outputs
dist/
build/
out/
.output/

# Test coverage reports — generated bởi jest --coverage / vitest run --coverage
coverage/
backend/coverage/
frontend/coverage/

# DB backups (Phase 40+ pre-migration mysqldump) — không commit
backups/
*.sql.bak

# Logs — dùng absolute path trong logger.js để tránh ghi ra root
*.log
logs/
backend/logs/
!backend/logs/.gitkeep

# Runtime-generated data (vector store, rebuild via npm run ai:rebuild-vectors)
backend/data/vector-db.json
backend/data/vector-db.json.bak

# Upload files — không commit binary files vào git
backend/uploads/*
!backend/uploads/.gitkeep
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS specific
.DS_Store
Thumbs.db

# IDEs
.idea/
.vscode/

# Temp
tmp/
temp/

# Mockup HTML tạm (so sánh variant redesign admin) — xóa sau khi rollout xong
frontend/public/_mock-*.html

# Claude Code
.claude/

# Temp commit-rewrite files
scripts/msg*.txt

# Lint/tool output
**/eslint_output.txt

# Analysis files
ANALYSIS_INDEX.md
ANALYSIS_REPORT.md
ANALYSIS_SUMMARY.md
QUICK_SUMMARY.txt
README_ANALYSIS.txt
export_docx.py
HANDOFF.md

dump.rdb
*_token.txt
backend/server_err.txt
backend/server_out*.log
backend/src/modules/uploads/

# Debug/audit screenshots (chrome-devtools) — không commit binary bloat
.screenshots/
frontend/.screenshots/
# Junk jest output (broken Windows path filename)
frontend/C*Tempjest_out*.txt
backend/coverage-tmp/

# LaTeX build artifacts (generated — không commit)
docs/*.aux
docs/*.bbl
docs/*.blg
docs/*.fdb_latexmk
docs/*.fls
docs/*.synctex.gz
docs/*.pdf
docs/*.out
docs/*.toc
docs/*.lof
docs/*.lot
docs/*.log
docs/.ruff_cache/
# Thesis images — nặng (~13MB), để untracked (commit source text only)
docs/figures/
docs/admin-redesign/

# Junk/temp/binaries — KHÔNG commit (session tooling, slides, backups)
.tools/
mermaid-cli-*/
unpacked/
*.tmp
*.pptx
*.zip
*.bak
.~lock*
